import { Meta, Story, Canvas, Props } from "@storybook/addon-docs";
import dayjs from "dayjs";
import { GradeCircularProgressBar } from "./GradeCircularProgressBar.tsx";

<Meta title="GradeCircularProgressBar" component={GradeCircularProgressBar} />

<Canvas>
  <Story name="default">
    <div style={{ width: "50px", height: "50px" }}>
      <GradeCircularProgressBar percentage={60} grade={66} />
    </div>
  </Story>
</Canvas>

## Props

<Props of={GradeCircularProgressBar} />
